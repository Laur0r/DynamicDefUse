plugins {
    id 'java-library'
    id 'maven-publish'
}

version '0.1.0'

dependencies {
    implementation project(':mulib')
    implementation 'org.ow2.asm:asm:7.0'
    implementation 'org.ow2.asm:asm-util:7.0'
    implementation 'org.ow2.asm:asm-commons:7.0'
    implementation 'junit:junit:4.13.2'
    implementation 'jakarta.xml.bind:jakarta.xml.bind-api:3.0.0'
    implementation 'com.sun.xml.bind:jaxb-impl:3.0.0'
    implementation 'com.github.javaparser:javaparser-symbol-solver-core:3.24.7'
    implementation 'org.yaml:snakeyaml:2.2'
    implementation project(':dacite-lsp')

}

jar {
    manifest {
        attributes 'Premain-Class': 'dacite.core.instrumentation.DaciteAgent'
        attributes 'Main-Class': 'dacite.core.DaciteLauncher'
        attributes 'Class-Path': configurations.runtimeClasspath.collect { it.name }.join(' ')
    }
}

test {
    useJUnitPlatform()
}

publishing {
    repositories {
        maven {
            name = 'GitHubPackages'
            url = uri('https://maven.pkg.github.com/dacite-defuse/dacite-core')
            credentials {
                username = System.getenv('GITHUB_ACTOR')
                password = System.getenv('GITHUB_TOKEN')
            }
        }
    }

    publications {
        maven(MavenPublication) {
            artifactId = 'dacite-core'
            from components.java
        }
    }
}
